import styled from 'styled-components/macro';
import { FixedSizes } from 'types/global-types';

const LegacyBackofficeWrapper = styled.div({
  marginTop: `${FixedSizes.headerHeight}px`,
  height: '99vh',
});

export default LegacyBackofficeWrapper;
